import React from 'react';
import { View, Text, ScrollView } from 'react-native';

const ProgramView = () => {
  return (
    <ScrollView>
      <Text>This is the ProgramView Screen</Text>
    </ScrollView>
  );
};

export default ProgramView;
